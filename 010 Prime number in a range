#include<iostream>
using namespace std;
int main()
{
    int a,b,count;
    cout << "Enter the first number of the range : ";
    cin >> a;
    cout << "Enter the last number of the range : ";
    cin >> b;
    for(int i=a; i<=b; i++)
    {
        count = 0;
        for(int j=1; j<=i; j++)
        {
            if(i%j==0)
            {
                count = count + 1;
            }
        }
                if(count == 2)
            {
                cout << i << " ";
            }
    }
    return 0;
}
