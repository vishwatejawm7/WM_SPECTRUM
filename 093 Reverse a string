#include<iostream>
#include<string>
using namespace std;
int main()
{
    string str;
    cout << "Enter a string : ";
    getline(cin, str);
    int l = str.length();
    cout << "Reversed string : ";
    for(int i=l; i>=0; --i)
    {
        cout << str[i];
    }
    return 0;
}
