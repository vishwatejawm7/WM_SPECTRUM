#include<iostream>
using namespace std;
int main()
{
    int a;
    cout << "Enter a number to check even or odd : ";
    cin >> a;
    if(a%2 == 0)
        cout << "even" ;else cout << "odd";
    return 0;
}
