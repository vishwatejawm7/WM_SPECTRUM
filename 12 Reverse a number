#include<iostream>
using namespace std;
int main()
{
    int a,sum{0};
    cout << "Enter a number to reverse it : ";
    cin >> a;
    while(a!=0)
    {
        sum = 0;
        sum = a%10;
        a = a/10;
        cout << sum;
    }
    return 0;
}
